# ======================
# Node / frontend
# ======================
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
dist/
build/

# ======================
# Terraform
# ======================
# states, locks, diretório local do provider
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
# planos binários
*tfplan
*.tfplan
*.tfplan.*
# variáveis locais/sensíveis
terraform.tfvars
terraform.tfvars.* 
!*/terraform/terraform.tfvars.example
# backup
terraform.tfstate.backup

# ======================
# Ansible
# ======================
*.retry
# inventários contendo IPs/credenciais (se quiser manter fora do repo)
*/ansible/inventory/*
!*/ansible/inventory/*.example
!*/ansible/inventory/*.template

*/ansible/inventories/*
!*/ansible/inventories/*.example
!*/ansible/inventories/*.template

# Ignorar variáveis sensíveis em group_vars e host_vars
*/ansible/group_vars/*/*.yml
*/ansible/group_vars/*.yml
*/ansible/host_vars/*/*.yml
*/ansible/host_vars/*.yml

# Permitir arquivos de exemplo
!*/ansible/group_vars/*.yml.example
!*/ansible/group_vars/*/*.yml.example
!*/ansible/host_vars/*.yml.example
!*/ansible/host_vars/*/*.yml.example

# Permitir vault.yml (sempre criptografado)
!*/ansible/group_vars/vault.yml

# Vault
*.vault.pass
vault_pass.txt
vault_pass.sh
.vault_pass*
**/vault_pass.*
!.vault.example.txt
!vault_pass.sh.example

# configurações locais do Ansible
ansible.cfg

# ======================
# Chaves privadas / certificados / segredos
# ======================
# nunca commitar chaves privadas, arquivos PEM, certificados e stores
*.pem
*.key
*.crt
*.csr
*.pfx
*.p12
*.jks
# variáveis de ambiente
.env
.env.*.local
.env.*.secret

# ======================
# Editor / OS
# ======================
# macOS
.DS_Store
# Windows
Thumbs.db
# VSCode / IDEs
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# ======================
# Logs / tmp / coverage
# ======================
logs
*.log
*.out
*.err
tmp/
temp/
coverage/

# ======================
# Outros
# ======================
*.bak
*.swp
*.zip
